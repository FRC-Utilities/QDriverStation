TODO:

- Get RAM and CPU usage from the roboRIO
- Test joystick POVs on a robot
- Implement a layer over SDL in order to obtain the same joystick mappings as the ones used by the official FRC Driver Station
- Implement unit tests, at least in the DS library and the Gamepad Manager
